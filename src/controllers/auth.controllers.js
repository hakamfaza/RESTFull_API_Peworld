const cryoto = require('crypto');
const bcrypt = require('bcrypt');
const { v4: uuidv4 } = require('uuid');

module.exports = {
  register: (req, res) => {
    try {

    } catch (error) {

    }
  },
};
